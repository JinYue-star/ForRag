# Conda 环境：在仓库根目录执行
#   conda env create -f environment.yml
# 更新依赖（改 requirements.txt 后）：
#   conda activate forrag
#   pip install -r requirements.txt
name: forrag
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  - pip:
      - -r file:requirements.txt
