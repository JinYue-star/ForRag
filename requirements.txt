# ForRag：文档问答 / RAG / 会话 / 测验（与 fastapi_service.py、doc_qa_assistant.py 对齐）
# 安装：python -m pip install -r requirements.txt
# 建议使用 conda/venv；若使用系统 Python，请保证 pip 与运行 uvicorn 的为同一解释器。

# ---------- Web API ----------
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic>=2.0.0
starlette>=0.37.0
python-dotenv>=1.0.0

# ---------- 持久化与向量库 ----------
chromadb>=0.4.24

# ---------- HTTP 客户端（千问 API / OpenAI 兼容）----------
openai>=1.40.0
httpx>=0.27.0

# ---------- 本地嵌入与向量检索 ----------
sentence-transformers>=3.0.0
faiss-cpu>=1.8.0
# 与 scipy / sklearn 等 wheels 二进制度兼容
numpy>=1.26,<2.0
tqdm>=4.66.0

# ---------- 本地大模型（可选路径；API 模式可不加载）----------
torch>=2.1.0
transformers>=4.44.0
accelerate>=0.33.0
sentencepiece>=0.2.0
modelscope>=1.14.0

# ---------- 文档解析 ----------
pymupdf>=1.24.0
python-docx>=1.1.0
openpyxl>=3.1.0
python-pptx>=0.6.23
pandas>=2.0.0

# ---------- 图像 OCR（上传图片入库）----------
pillow>=10.0.0
onnxruntime>=1.16.0
rapidocr-onnxruntime>=1.3.0

# ---------- 测试 ----------
pytest>=8.0.0
